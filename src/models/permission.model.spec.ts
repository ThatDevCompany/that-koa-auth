import { ExamplePermission } from './permission.model'

/**
 * Tests for Permission
 */
describe('Permission', () => {
	/**
	 * General Tests
	 */
	it('should be defined', async () => {
		expect(ExamplePermission).toBeDefined()
	})
})
