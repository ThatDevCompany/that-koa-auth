import { ExampleTenant } from './tenant.model'

/**
 * Tests for Tenant
 */
describe('Tenant', () => {
	/**
	 * General Tests
	 */
	it('should be defined', async () => {
		expect(ExampleTenant).toBeDefined()
	})
})
