import { authorize } from './authorize'

/**
 * Tests for authorize
 */
describe('authorize', () => {
	/**
	 * General Tests
	 */
	it('should be defined', async () => {
		expect(authorize).toBeDefined()
	})
})
