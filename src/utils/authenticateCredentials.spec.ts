import { authenticateCredentials } from './authenticateCredentials'

/**
 * Tests for authenticateCredentials
 */
describe('authenticateCredentials', () => {
	/**
	 * General Tests
	 */
	it('should be defined', async () => {
		expect(authenticateCredentials).toBeDefined()
	})
})
